general:
    branches:
        only:
            - master
test:
    override:
        - (echo "build rtmp-server" && cd trunk && cmake . && make)
        - (echo "run unit test" && cd trunk && ./objs/utest_rs_server)
